#include "main.h"

/**
 * _islower - returns 1 if c is lowercase
 * returns 0 if not
 * @c: file to be checked
 * Return: 1 if c is lowercase, 0 if otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
